USE lindenmayer;
//@demo_for=fractal, plotting
//*Draws a fractal plant
main->begin
  setOptions([['axisStyleX',0],['axisStyleY',0]]);
  plot(lindenmayerTurtle(PLANT,6));
  renderToFile(replace(myPath,'.mnh','.png'),768,768,3);
end;
