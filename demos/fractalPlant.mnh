USE lindenmayer;
//*Draws a fractal plant
main->plot.setOptions([['axisStyleX',0],['axisStyleY',0]]) and
      plot.plot(lindenmayerTurtle(PLANT,6)) and
      plot.renderToFile(replace(myPath,'.mnh','.png'),768,768,8);
