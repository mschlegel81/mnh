private gcd(a,0)->a;
private gcd(a,b)->gcd(b, a mod b);

//*For regression test
main->main(1238124,1236);
main(x,y)->printf('The greatest common divisor of %{$0}s and %{$1}s is %{gcd($0,$1)}s',softCast(x),softCast(y));
