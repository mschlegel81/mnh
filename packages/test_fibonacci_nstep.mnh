fib_nstep(i>=n,n>=1)->each(j,[1..n],+,fib_nstep(i-j,n));
fib_nstep(i   ,n>=1)->1;
CACHE fib_nstep;

fib(i>1)->fib(i-1)+fib(i-2);
fib(i)->1;

print(" n | values") and
print(
  each(n,[1..10],&"\n"&,n&"\t| "&each(i,[1..20],&"\t"&, fib_nstep(i+n-3,n)))
 );