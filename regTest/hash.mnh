fileHash(filename:string)->fileExists(filename) ? hash(fileContents(filename))&'' : '--file does not exist--';
main->print(fileHash(myPath));
main(...)->printf("%X%\t%X%",$params,each(p,$params,fileHash(p)));
